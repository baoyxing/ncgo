path: internal/service/service.go
update_behavior:
  type: skip
body: |-
  package Service
  
  import (
     "{{.Module}}/internal/base/conf"
     "github.com/cloudwego/kitex/pkg/klog"
  )
  
  type Service struct {
    log  klog.CtxLogger
    conf *conf.Config
  }
  
  func NewService(c *conf.Config,log klog.CtxLogger) {
    return &Service{  
        log:log,
        conf:c,
    }
  }