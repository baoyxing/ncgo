path: internal/base/data/provider.go
update_behavior:
  type: skip
body: |-
  package data
  
  import (
      "github.com/google/wire"
      )

      var ProviderSet = wire.NewSet(
        NewData,
    )